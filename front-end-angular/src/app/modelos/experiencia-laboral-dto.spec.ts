import { ExperienciaLaboralDto } from './experiencia-laboral-dto';

describe('ExperienciaLaboralDto', () => {
  it('should create an instance', () => {
    expect(new ExperienciaLaboralDto()).toBeTruthy();
  });
});
