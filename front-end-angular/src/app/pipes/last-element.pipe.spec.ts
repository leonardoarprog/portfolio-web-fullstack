import { LastElementPipe } from './last-element.pipe';

describe('LastElementPipe', () => {
  it('create an instance', () => {
    const pipe = new LastElementPipe();
    expect(pipe).toBeTruthy();
  });
});
