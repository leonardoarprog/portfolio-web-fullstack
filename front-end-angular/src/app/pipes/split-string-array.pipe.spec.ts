import { SplitStringArrayPipe } from './split-string-array.pipe';

describe('SplitStringArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitStringArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
