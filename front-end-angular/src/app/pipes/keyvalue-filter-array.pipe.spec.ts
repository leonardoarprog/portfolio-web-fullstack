import { KeyvalueFilterArrayPipe } from './keyvalue-filter-array.pipe';

describe('KeyvalueFilterArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new KeyvalueFilterArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
