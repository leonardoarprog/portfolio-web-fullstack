import { DeletePropertyPipe } from './delete-property.pipe';

describe('DeletePropertyPipe', () => {
  it('create an instance', () => {
    const pipe = new DeletePropertyPipe();
    expect(pipe).toBeTruthy();
  });
});
