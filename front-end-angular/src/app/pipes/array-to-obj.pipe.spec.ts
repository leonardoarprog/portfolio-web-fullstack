import { ArrayToObjPipe } from './array-to-obj.pipe';

describe('ArrayToObjPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayToObjPipe();
    expect(pipe).toBeTruthy();
  });
});
